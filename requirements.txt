# OCR / PDF
pymupdf>=1.24.0
easyocr>=1.7.0
# EasyOCR requires PyTorch. On GPU machine use setup_gpu.sh OR install CUDA PyTorch FIRST:
#   CUDA 12.x (RTX 30xx/40xx/50xx, driver 12.0+):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   CUDA 11.8 (старые карты):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   CPU only (медленно):
#     pip install torch torchvision
# ВАЖНО: если установить easyocr ДО PyTorch с CUDA, pip поставит CPU-torch из PyPI.
# Для исправления: pip install torch torchvision --index-url .../cu124 --force-reinstall

# Extraction LLM backend (Ollama)
ollama>=0.4.0

# Web app
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0

# Database
psycopg2-binary>=2.9.9
sshtunnel>=0.4.0
paramiko>=3.0.0,<4.0.0

# Config
python-dotenv>=1.0.0

# GigaChat extraction backend (optional; needed when EXTRACTION_BACKEND=gigachat)
langchain-community>=0.2.0
langchain-core>=0.2.0
gigachat>=0.1.15
