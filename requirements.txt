# OCR / PDF
pymupdf>=1.24.0
easyocr>=1.7.0
# EasyOCR requires PyTorch. On GPU machine install CUDA build FIRST (before pip install -r):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# CPU only (slow, not recommended):
#   pip install torch torchvision

# Extraction LLM backend (Ollama)
ollama>=0.4.0

# Web app
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0

# Database
psycopg2-binary>=2.9.9
sshtunnel>=0.4.0
paramiko>=3.0.0,<4.0.0

# Config
python-dotenv>=1.0.0

# GigaChat extraction backend (optional; needed when EXTRACTION_BACKEND=gigachat)
langchain-community>=0.2.0
langchain-core>=0.2.0
gigachat>=0.1.15
